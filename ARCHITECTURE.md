# 🗺️ GlobeTrotter Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React + Vite)                   │
│  localhost:5173                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  React Components (Pages & Layout)                         │ │
│  │  ├─ Dashboard    ├─ LoginPage   ├─ SignupPage            │ │
│  │  ├─ TripList     ├─ CreateTrip  ├─ TripDetails           │ │
│  │  ├─ ItineraryBuilder           ├─ CitySearch             │ │
│  │  ├─ ActivitySearch             ├─ BudgetView             │ │
│  │  └─ Profile                                               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↑                                    │
│                    AppContext (State)                            │
│                    src/context/AppContext.jsx                    │
│                              ↓                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  API Service Client (Axios)                               │ │
│  │  src/services/api.js                                      │ │
│  │  • Token management                                       │ │
│  │  • Auto-inject JWT in headers                             │ │
│  │  • Error interceptors                                     │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
                              ↓
                    HTTP/REST API Requests
                         (Port 5000)
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│                      BACKEND (Node + Express)                    │
│  localhost:5000                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Express Server (server.js)                               │ │
│  │  • CORS enabled                                           │ │
│  │  • JSON body parser                                       │ │
│  │  • Request routing                                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Routes (Route Controllers)                               │ │
│  │  ├─ /api/users      → userRoutes.js                       │ │
│  │  ├─ /api/cities     → cityRoutes.js                       │ │
│  │  ├─ /api/activities → activityRoutes.js                   │ │
│  │  └─ /api/trips      → tripRoutes.js                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Middleware                                               │ │
│  │  ├─ JWT Verification (auth.js)                            │ │
│  │  ├─ Error Handling                                        │ │
│  │  └─ CORS Headers                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Controllers (Business Logic)                             │ │
│  │  ├─ userController.js    (Auth logic)                     │ │
│  │  ├─ cityController.js    (City CRUD)                      │ │
│  │  ├─ activityController.js (Activity CRUD)                 │ │
│  │  └─ tripController.js    (Trip & Stop CRUD)               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  MongoDB Models (Schemas)                                 │ │
│  │  ├─ User.js        (Accounts & Profiles)                  │ │
│  │  ├─ City.js        (Destinations)                         │ │
│  │  ├─ Activity.js    (Things to Do)                         │ │
│  │  ├─ Trip.js        (Travel Plans)                         │ │
│  │  └─ Stop.js        (Trip Legs)                            │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│                    DATABASE (MongoDB)                            │
│                                                                   │
│  Collections:                                                   │
│  ├─ users          (User accounts with hashed passwords)       │
│  ├─ cities         (8 sample destinations)                     │
│  ├─ activities     (10+ activities across cities)              │
│  ├─ trips          (User travel plans)                         │
│  └─ stops          (Individual stops within trips)             │
│                                                                   │
│  Storage: mongodb://localhost:27017/globetrotter               │
│  (or MongoDB Atlas cloud)                                       │
└──────────────────────────────────────────────────────────────────┘
```

## Authentication Flow

```
┌─────────────┐
│   Frontend  │
└──────┬──────┘
       │
       │ 1. User clicks "Sign Up"
       ↓
┌──────────────────────────────────┐
│ Registration Form                │
│ • Name, Email, Password          │
└──────┬───────────────────────────┘
       │
       │ 2. POST /api/users/register
       │    { name, email, password }
       ↓
┌──────────────────────────────────┐
│ Backend - userController         │
│ • Validate input                 │
│ • Hash password with bcryptjs    │
│ • Create User in MongoDB         │
│ • Sign JWT token                 │
└──────┬───────────────────────────┘
       │
       │ 3. Response with JWT
       │    { token, user }
       ↓
┌──────────────────────────────────┐
│ Frontend - localStorage           │
│ • Save token                     │
│ • Update user state              │
│ • Redirect to Dashboard          │
└──────┬───────────────────────────┘
       │
       │ 4. Future requests
       │    Authorization: Bearer <token>
       ↓
┌──────────────────────────────────┐
│ Backend - auth middleware        │
│ • Verify JWT signature           │
│ • Extract user ID                │
│ • Attach to request              │
│ • Continue to controller         │
└──────────────────────────────────┘
```

## Data Flow - Creating a Trip

```
User creates trip:
│
├─ Frontend Form Input
│  • Trip name: "Paris Adventure"
│  • Dates: 2024-06-01 to 2024-06-30
│  • Budget: 200000 (₹)
│
└─ POST /api/trips
   Headers: Authorization: Bearer <token>
   Body: { name, startDate, endDate, totalBudgetINR }
      ↓
   ┌─────────────────────────────┐
   │ tripController.createTrip() │
   │ • Verify token              │
   │ • Validate input            │
   │ • Create Trip document      │
   │ • Save to MongoDB           │
   │ • Return created trip       │
   └──────────┬──────────────────┘
              ↓
   ┌──────────────────────────────┐
   │ MongoDB Trip Document        │
   │ {                            │
   │   _id: "...",                │
   │   userId: "...",             │
   │   name: "Paris Adventure",   │
   │   startDate: 2024-06-01,     │
   │   endDate: 2024-06-30,       │
   │   totalBudgetINR: 200000,    │
   │   stops: [],                 │
   │   status: "Planning",        │
   │   createdAt: ...             │
   │ }                            │
   └──────────┬──────────────────┘
              ↓
   Frontend receives response
      ├─ AppContext updated
      ├─ UI refreshes
      └─ Trip appears in list
```

## Request/Response Cycle

```
Frontend Request:
┌────────────────────────────────┐
│ axios.post('/api/cities', ...) │
└────────┬───────────────────────┘
         ↓
API Interceptor adds token:
┌──────────────────────────────────────────┐
│ Headers: {                               │
│   Authorization: Bearer eyJhbGc...      │
│   Content-Type: application/json        │
│ }                                        │
└────────┬─────────────────────────────────┘
         ↓
Express receives request:
┌──────────────────────────────────────────┐
│ POST /api/cities                         │
│ Check CORS headers                       │
│ Parse JSON body                          │
│ Pass to middleware                       │
└────────┬─────────────────────────────────┘
         ↓
JWT Middleware verifies token:
┌──────────────────────────────────────────┐
│ • Extract token from header              │
│ • Verify signature with JWT_SECRET       │
│ • Extract userId                         │
│ • Attach to req.userId                   │
│ • Continue to next middleware            │
└────────┬─────────────────────────────────┘
         ↓
Route Controller executes:
┌──────────────────────────────────────────┐
│ userController.createCity()              │
│ • Access request data                    │
│ • Access req.userId from middleware      │
│ • Validate inputs                        │
│ • Query/update MongoDB                   │
│ • Return result                          │
└────────┬─────────────────────────────────┘
         ↓
Send response back:
┌──────────────────────────────────────────┐
│ res.status(201).json({                   │
│   _id: "...",                            │
│   name: "Paris",                         │
│   costIndexINR: 4500,                    │
│   ...                                    │
│ })                                       │
└────────┬─────────────────────────────────┘
         ↓
Frontend receives response:
┌────────────────────────────────┐
│ • Status: 201 (Created)        │
│ • Data in response.data        │
│ • Update AppContext            │
│ • UI re-renders                │
│ • Show success message         │
└────────────────────────────────┘
```

## Technology Stack

```
Frontend:
┌─────────────────────────────────┐
│ React 18.2                      │
├─────────────────────────────────┤
│ React Router v6 (Navigation)    │
├─────────────────────────────────┤
│ Vite (Dev server & bundler)     │
├─────────────────────────────────┤
│ Tailwind CSS (Styling)          │
├─────────────────────────────────┤
│ Lucide React (Icons)            │
├─────────────────────────────────┤
│ Axios (HTTP client)             │
├─────────────────────────────────┤
│ Date-fns (Date utilities)       │
└─────────────────────────────────┘

Backend:
┌─────────────────────────────────┐
│ Node.js v14+                    │
├─────────────────────────────────┤
│ Express.js (Web framework)      │
├─────────────────────────────────┤
│ MongoDB (Database)              │
├─────────────────────────────────┤
│ Mongoose (ODM)                  │
├─────────────────────────────────┤
│ JWT (Authentication)            │
├─────────────────────────────────┤
│ bcryptjs (Password hashing)     │
├─────────────────────────────────┤
│ CORS (Cross-origin)             │
├─────────────────────────────────┤
│ Dotenv (Environment config)     │
└─────────────────────────────────┘
```

## File Organization

```
globetrotter-app/
│
├── Frontend Source
│   └── src/
│       ├── context/
│       │   └── AppContext.jsx (State management + API calls)
│       ├── services/
│       │   └── api.js (Axios instance + API modules)
│       ├── pages/
│       │   ├── Dashboard.jsx (Home)
│       │   ├── LoginPage.jsx (Authentication)
│       │   ├── CreateTrip.jsx (New trip)
│       │   ├── TripDetails.jsx (Trip overview)
│       │   ├── ItineraryBuilder.jsx (Add stops)
│       │   ├── BudgetView.jsx (Budget tracking)
│       │   ├── CitySearch.jsx (Browse cities)
│       │   ├── ActivitySearch.jsx (Browse activities)
│       │   └── ...
│       └── ...
│
├── Backend Source
│   └── backend/
│       ├── server.js (Express setup)
│       ├── seed.js (Database seeding)
│       │
│       ├── controllers/
│       │   ├── userController.js (Auth logic)
│       │   ├── cityController.js (City CRUD)
│       │   ├── activityController.js (Activity CRUD)
│       │   └── tripController.js (Trip CRUD)
│       │
│       ├── models/
│       │   ├── User.js
│       │   ├── City.js
│       │   ├── Activity.js
│       │   ├── Trip.js
│       │   └── Stop.js
│       │
│       ├── routes/
│       │   ├── userRoutes.js
│       │   ├── cityRoutes.js
│       │   ├── activityRoutes.js
│       │   └── tripRoutes.js
│       │
│       ├── middleware/
│       │   └── auth.js (JWT verification)
│       │
│       ├── package.json
│       ├── .env
│       └── README.md
│
└── Documentation
    ├── SETUP.md (Installation guide)
    ├── QUICK_START.md (Quick reference)
    ├── BACKEND_SUMMARY.md (Implementation details)
    └── COMPLETION_SUMMARY.txt (This overview)
```

## Database Relationships

```
┌────────────┐
│   User     │
│ ──────────│
│ _id       │ (unique)
│ name      │
│ email     │ (unique)
│ password  │ (hashed)
└────┬───────┘
     │ (1 user has many trips)
     │
     ↓
┌────────────────────┐
│   Trip             │
│ ────────────────── │
│ _id                │
│ userId (foreign)   │ ──→ User
│ name               │
│ totalBudgetINR     │ (₹)
│ startDate          │
│ endDate            │
│ stops: [ObjectId]  │
└────┬───────────────┘
     │ (1 trip has many stops)
     │
     ↓
┌────────────────────────┐      ┌────────────┐
│   Stop                 │      │   City     │
│ ────────────────────── │      │ ──────────│
│ _id                    │      │ _id       │
│ tripId (foreign) ──────┼─→ Trip         │ name
│ cityId (foreign) ──────┼────→ country   │
│ city: String           │      region    │
│ startDate              │      costIndexINR
│ endDate                │      popularity
│ activities: [ObjectId] │
└────┬────────────────────┘
     │ (1 stop has many activities)
     │
     ↓
┌──────────────────┐
│   Activity       │
│ ────────────────│
│ _id              │
│ cityId (foreign) │ ──→ City
│ name             │
│ category         │
│ costINR          │ (₹)
│ duration (hours) │
│ rating (0-5)     │
└──────────────────┘
```

---

This architecture ensures:
✅ Clear separation of concerns
✅ Scalable structure
✅ Easy testing
✅ Professional practices
✅ Dynamic data flow
✅ Secure authentication
